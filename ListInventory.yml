- hosts: all
  connection: local  
  gather_facts: no

  tasks:
    - debug: msg="groups={{groups}}"
      run_once: true

- hosts: all
  connection: local   
  gather_facts: no


  tasks:
   - debug: msg="play_hosts={{play_hosts}}"
     run_once: true